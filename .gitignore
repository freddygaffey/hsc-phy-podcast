# Dependencies
node_modules/

# Wrangler (Cloudflare CLI) — local state, cache, and logs
.wrangler/

# Build output (regenerated by tools/deploy.sh)
dist/

# Python TTS pipeline
.tts-venv/
__pycache__/
*.pyc

# Rendered episode audio — large; served from R2, never committed
content/**/*.m4a

# Audio uploader cache (tracks what's already in R2)
tools/.upload-state.json

# Logs
*.log
run.log

# Local-only reference material (teacher slides, textbook, data sheets) — never pushed
facts/
*.pdf
*.PDF

# macOS
.DS_Store
